---
description: Gap-targeted CLAUDE.md depth interview — 7 questions to add context that pre-drafting couldn't capture
---

Read the full skill instructions at `05 Skills/deep-context-fill.md` and execute the workflow.

Walk pluto through the 7 questions one at a time, waiting for each answer. Then append the structured output as a new section `## Deep context (deep-context-fill output, [DATE])` to the vault root `CLAUDE.md`. Do NOT overwrite existing sections.

If a deep-context section already exists in CLAUDE.md, ask whether to append a new dated section, replace, or skip — don't silently overwrite.
