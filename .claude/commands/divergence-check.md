---
description: Bias-check pass — counter-arguments, tensions with other notes, data gaps on concept and synthesis pages
argument-hint: [optional: target page or folder]
---

Read the full skill instructions at `05 Skills/divergence-check.md` and run the divergence check.

If $ARGUMENTS is provided, scope the check to that note or folder. Otherwise scan the whole `00 Notes/concepts/` and `00 Notes/research/` directories.

For each target: generate strongest counter-arguments, surface tensions with other notes in the vault, identify data gaps, append findings to a "## Counter-Arguments & Data Gaps" section on the page. Do NOT rewrite existing claims — only append.

Run quarterly or after a one-sided source ingestion (e.g. you just consumed 5 articles all making the same case).
