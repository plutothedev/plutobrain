---
description: Interview-driven project creation under 03 Projects/
argument-hint: [optional project name]
---

Read the full skill instructions at `05 Skills/new-project.md` and execute the project-creation workflow.

If pluto provided a project name as argument: $ARGUMENTS — use it as the working name. Otherwise, ask.

Create the project structure under `03 Projects/<slug>/` with a project CLAUDE.md, appropriate subfolders, and any initial files the interview surfaces. Update vault root `CLAUDE.md` to add the new project under "Active projects". Update `index.md` to list the new project.

If the new project competes with existing open loops, surface the relevant pattern from `patterns.md` (especially Building Over Finishing and Impulsive Project Initiation Under Stimulus). Do not block — name the pattern and let pluto choose with awareness.
